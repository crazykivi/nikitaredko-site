services:
  app:
    image: ${IMAGE_NAME:-nikitaredko-site:local}
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ${CONTAINER_NAME:-nikitaredko-site}
    ports:
      - "${PORT:-8080}:8080"
    env_file:
      - .env
    restart: unless-stopped